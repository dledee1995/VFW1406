var windowProperties = {
	title : "Window",
	backgroundColor : "#050",
	fullscreen : true
};
var mainWindow = Ti.UI.createWindow(windowProperties);


mainWindow.open();

var loadfile = require("additionals");

